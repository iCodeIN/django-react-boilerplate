import { useEffect } from "react";
// import { useDispatch } from "react-redux";

export function InitApp() {
//   const dispatch = useDispatch();
  useEffect(() => {
    // dispatch(restoreFavorites()); // get CSRF
  });

  return null;
}
